import React from "react";

const Pill = ({ text }) => {
  return <div className="bg-white p-1 px-3 rounded-full bg-opacity-30 text-white w-fit text-sm capitalize">{text}</div>;
};

export default Pill;
